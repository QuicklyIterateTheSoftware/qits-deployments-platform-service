# The release pipeline: this repository reacts to its OWN release and pushes the image under the
# released VERSION. qits-events' file, adapted to this repository's build — see that repository for
# the long form of each rule; the short form of the ones that bite:
#
# - THE SELECTION NAMING THIS REPOSITORY IS NOT A LOOP. `SCMRelease` says only "source control has
#   this version" and qits-projects' Auto Release publishes it; nothing in this file publishes it. The event
#   that could loop is SoftwareRelease, and this file neither listens for it nor publishes it.
# - THE VERSION TAG IS THE ONLY COORDINATE. The per-push `:$QITS_CI_SHA` image went with
#   ci-post-receive.yml; nothing builds a branch and nothing deploys one. What this pipeline pushes
#   is what the deployer pulls — this repository's own contract with itself — and a Deployment
#   Request names that version, never a sha.
# - `artifacts:` IS A DECLARATION, NOT A REPORT. qits-ci never learns how to publish anything, so
#   it cannot see what the step pushed; it announces on the strength of the line below. One entry,
#   because this pipeline pushes one image: the jars this repo builds stay inside it and no maven
#   release publishes them.
# - THE NAME IS UNQUALIFIED ON PURPOSE — the registry is `qits-platform-artifacts:8080` inside a
#   step container and `registry.dev.localhost:8080` outside, and an OCI reference cannot carry a
#   path prefix.
# - THE BUILD BELOW IS ci-event-release-request.yml's GATING STEP, AND THE TWO MUST STAY MATCHED —
#   the QA pipeline proves the fold with this same build, minus the push. That file is where the
#   two halves, the submodule fetch, the lockfile origin swap, the `npm_config_*` environment and
#   the `--network host` doctrine are explained at length; this one carries the short form and
#   points there rather than keeping a second copy of the reasoning to drift.
# - THIS REPO IS ALSO THE DEPLOYER, so its own release deploys itself through the handoff README
#   calls "Self-update is a handoff": successor started, detached referee arbitrates, the surviving
#   instance's sweep records the outcome. Nothing in this file changes for that — it is worth
#   knowing because the /platform-deployments surface blips mid-cutover.
# - THIS FILE IS INERT UNTIL an SCMRelease for this repository exists, and it lands first on
#   purpose: the bus is at-most-once with no catch-up and no replay, so a trigger file must be in
#   place before the event it selects.
event: SCMRelease
when:
  - repository: { exact: qits-deployments-platform-service }
artifacts:
  - { type: docker, name: qits/qits-deployments }
steps:
    # node-docker-base, the image ci-event-release-request.yml already uses: node, npm and git for half one,
    # the docker CLI for half two — and node is also what reads the event payload, which is JSON.
  - image: qits/build-images/node-docker-base:latest
    docker: true
    timeout-seconds: 3600
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this an early failure would be
      # swallowed by the next command's exit code — and a green release run is what makes qits-ci
      # announce the artifact, so it has to mean the image really was built and pushed. -u is
      # load-bearing too: an unset registry var must stop the step, not quietly resolve the @qits
      # scope against npmjs.org, where these packages do not exist and never will.
      set -eu

      # THE EVENT, READ AS DATA. The payload reaches the container verbatim as one string in
      # $QITS_EVENT_PAYLOAD; there is no per-field projection.
      version=$(node -p 'JSON.parse(process.env.QITS_EVENT_PAYLOAD).version')
      # The version reaches a `sed` s### expression below, and `#` is that expression's own
      # delimiter — a version carrying one closes it, and GNU sed's `e` command then runs a shell.
      # Refused rather than quoted: a version here is a calver or a semver, and nothing legitimate
      # needs a character outside this set.
      case "$version" in
        ''|*[!0-9A-Za-z._+-]*) echo "refusing an implausible version: $version" >&2; exit 1 ;;
      esac
      echo "releasing qits/qits-deployments:$version"

      # THE TAG IS THE CHECKOUT TARGET, and it is the only reason this run needs one. An
      # event-triggered run is cloned at the head of main, which is where the release commit is
      # *now* — a later push moves that, and the tag does not, so a busy main cannot make this
      # pipeline ship a different tree than the one that was released. The clone is shallow and
      # carries no tags, hence the explicit refspec; the URL rather than `origin` because every
      # address a step needs arrives as environment. `checkout --detach` peels the annotated tag to
      # its commit — and it is what makes the submodule fetch below land on the
      # qits-deployments-platform-frontend gitlink the release recorded rather than on main's.
      git fetch "$QITS_CI_REPOSITORY_URL" "refs/tags/$version:refs/tags/$version"
      git checkout --detach "$version"

      # --- half one: the bundle -----------------------------------------------------------------
      # The daemon's shallow clone does not recurse, so initialise the packaged SPA explicitly.
      frontend_url="${QITS_CI_REPOSITORY_URL%/*}/qits-deployments-platform-frontend"
      git -c submodule.qits-deployments-platform-frontend.url="$frontend_url" \
        submodule update --init service/src/main/webui

      cd service/src/main/webui
      # TWO SERVICES since the byte-plane split, so the swap reads the PATH: an entry under the
      # hosted registry's own pathname ($QITS_NPM_REGISTRY_URL's, /artifacts/npm/npm/) gets the
      # hosted origin and everything else gets the proxy's, and the broad expression has to run
      # before the narrow one. ci-event-release-request.yml carries the full reasoning.
      npm_hosted_origin=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).origin')
      npm_proxy_origin=$(node -p 'new URL(process.env.QITS_NPM_PROXY_URL).origin')
      npm_hosted_path=$(node -p 'new URL(process.env.QITS_NPM_REGISTRY_URL).pathname')
      sed -i -E \
        -e "s#(\"resolved\": \")https?://[^/\"]+#\1$npm_proxy_origin#" \
        -e "s#(\"resolved\": \")https?://[^/\"]+($npm_hosted_path)#\1$npm_hosted_origin\2#" \
        package-lock.json

      env npm_config_registry="$QITS_NPM_PROXY_URL" \
          "npm_config_@qits:registry=$QITS_NPM_REGISTRY_URL" \
          npm ci --no-audit --no-fund

      npm run build
      cd -

      # --- half two: the image ------------------------------------------------------------------
      # Same doctrine as ci-event-release-request.yml: --network host, NOT a custom network, because buildkit
      # refuses custom networks on a build; the maven registry's host-published address is derived
      # from the same injected env the image tag uses, so no address is stated here.
      # Same doctrine for the credential: a SECRET MOUNT, never a build arg, because an arg is
      # recorded in the history of a pullable image. The launcher injects the run's commissioned idp
      # client as $QITS_COMMISSIONED_CLIENT_ID/$QITS_COMMISSIONED_CLIENT_SECRET. Unconditional — a
      # missing env source is NOT a buildx error, measured on v0.36.1 — so it is inert until a run
      # carries the pair. ci-event-release-request.yml has the same two flags; keep them matched.
      ref="$QITS_REGISTRY/$QITS_IMAGE_REPOSITORY/qits-deployments:$version"
      docker build --network host \
        --build-arg QITS_MAVEN_REPOSITORY_URL="http://$QITS_REGISTRY/artifacts/maven/maven" \
        --build-arg QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_CENTRAL_MIRROR_URL:-}" \
        --secret id=qits-client-id,env=QITS_COMMISSIONED_CLIENT_ID \
        --secret id=qits-client-secret,env=QITS_COMMISSIONED_CLIENT_SECRET \
        -t "$ref" -f docker/Dockerfile .
      docker push "$ref"
      # NO `docker rmi "$ref"`: on a CI host it frees nothing — the layers stay in the builder's
      # cache either way — while under the containerd image store it drops the last reference to
      # content a concurrent build of this same Dockerfile may still be resolving, which is the
      # race that broke the release of 2026-08-11.

      # --- the SBOM -----------------------------------------------------------------------------
      # THE DOCUMENT GOES UP AFTER THE PUSH AND BEFORE THIS STEP EXITS GREEN. qits-ci announces
      # `SoftwareRelease` only for a green run, so ordering is the whole guarantee: no consumer is
      # ever told about a version whose bill of materials is missing. A failed PUT fails the release
      # ON PURPOSE — the store being down is not a reason to announce a version nobody can audit.
      # Only THIS pipeline uploads one: the QA pipeline's fold build publishes nothing and
      # announces no release, so it owes no document.
      #
      # The second `docker build` reaches the Dockerfile's `sbom` stage — a `FROM scratch` holding
      # only the service module's CycloneDX document, exported with `--output` instead of pushed.
      # Args and secrets are BYTE-IDENTICAL to the image build above, which is what makes every
      # layer a cache hit and this export seconds rather than a second native compile; a drift
      # between the two invocations costs a full rebuild, not a wrong document.
      docker build --network host --target sbom --output type=local,dest=.sbom \
        --build-arg QITS_MAVEN_REPOSITORY_URL="http://$QITS_REGISTRY/artifacts/maven/maven" \
        --build-arg QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_CENTRAL_MIRROR_URL:-}" \
        --secret id=qits-client-id,env=QITS_COMMISSIONED_CLIENT_ID \
        --secret id=qits-client-secret,env=QITS_COMMISSIONED_CLIENT_SECRET \
        -f docker/Dockerfile .
      # The SBOM store is a sibling path of the npm registry in the qits-platform-artifacts
      # deployment, so its root is DERIVED from $QITS_NPM_REGISTRY_URL — the qits-net alias a STEP
      # container can reach. NOT from $QITS_REGISTRY: that is the host-published name the docker
      # daemon pushes through, and inside this container it answers curl with an instant connection
      # refusal (measured twice on qits-artifacts-service's 2026.902 releases — the pushes above
      # succeeded through the daemon while the same host name was unreachable from in here; the
      # edge name fares no better, it 401s a sessionless write). The path names the DECLARED
      # artifact identity verbatim: `artifacts:` above says `{type: docker, name:
      # qits/qits-deployments}`, and (packageType, packageName, version) is how the SoftwareRelease
      # consumer will ask for it.
      sbom_base="${QITS_NPM_REGISTRY_URL%%/artifacts/*}/artifacts/sboms"
      curl -fsS -X PUT -H 'Content-Type: application/vnd.cyclonedx+json' \
        --data-binary @.sbom/sbom.json \
        "$sbom_base/docker/qits/qits-deployments/-/$version"

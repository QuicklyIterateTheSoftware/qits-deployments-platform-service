# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service -am test -Dtest=OpenApiSchemaExportTest -Dsurefire.failIfNoSpecifiedTests=false` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    ApplicationSpec:
      type: object
      deprecated: true
      required:
      - repoId
      - name
      properties:
        repoId:
          type: string
          pattern: \S
        name:
          type: string
          pattern: \S
        healthPath:
          type: string
    CreateEnvironmentRequest:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          pattern: \S
        network:
          type: string
        platform:
          type: boolean
        applications:
          type: array
          deprecated: true
          items:
            $ref: "#/components/schemas/ApplicationSpec"
    DeploymentRequestResponse:
      type: object
      properties:
        deploymentRequest:
          $ref: "#/components/schemas/PdDeploymentRequestDto"
        deployment:
          $ref: "#/components/schemas/PdDeploymentDto"
    EnvironmentResponse:
      type: object
      properties:
        environment:
          $ref: "#/components/schemas/PdEnvironmentDto"
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    ListApplicationsResponse:
      type: object
      properties:
        applications:
          type: array
          items:
            $ref: "#/components/schemas/PdApplicationDto"
    ListDeploymentRequestsResponse:
      type: object
      properties:
        deploymentRequests:
          type: array
          items:
            $ref: "#/components/schemas/PdDeploymentRequestDto"
    ListDeploymentsResponse:
      type: object
      properties:
        deployments:
          type: array
          items:
            $ref: "#/components/schemas/PdDeploymentDto"
    ListEnvironmentsResponse:
      type: object
      properties:
        environments:
          type: array
          items:
            $ref: "#/components/schemas/PdEnvironmentDto"
    ListLinksResponse:
      type: object
      properties:
        services:
          type: array
          items:
            $ref: "#/components/schemas/PdLinkedServiceDto"
    ListPinsResponse:
      type: object
      properties:
        pins:
          type: array
          items:
            $ref: "#/components/schemas/PdPinDto"
    ListServicesResponse:
      type: object
      properties:
        services:
          type: array
          items:
            $ref: "#/components/schemas/PdServiceDto"
    PdApplicationDto:
      type: object
      properties:
        id:
          type: string
        repoId:
          type: string
        name:
          type: string
        environmentId:
          type: string
        environmentName:
          type: string
        target:
          $ref: "#/components/schemas/PdDeploymentTarget"
        availableOnEnv:
          type: boolean
        branch:
          type: string
        healthPath:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
    PdDeploymentDto:
      type: object
      properties:
        id:
          type: string
        applicationId:
          type: string
        applicationName:
          type: string
        version:
          type: string
        commitSha:
          type: string
        runId:
          type: string
        status:
          $ref: "#/components/schemas/PdDeploymentStatus"
        containerName:
          type: string
        detail:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
    PdDeploymentRequestDto:
      type: object
      properties:
        id:
          type: string
        applicationName:
          type: string
        version:
          type: string
        environmentId:
          type: string
        packageName:
          type: string
        repoId:
          type: string
        projectId:
          type: string
        qualityGate:
          $ref: "#/components/schemas/PdQualityGate"
        gateDetail:
          type: string
        deploymentId:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        gateSettledAt:
          $ref: "#/components/schemas/Instant"
        deploymentStatus:
          $ref: "#/components/schemas/PdDeploymentStatus"
    PdDeploymentStatus:
      type: string
      enum:
      - QUEUED
      - STARTING
      - ACTIVE
      - IMAGE_MISSING
      - SPEC_UNREADABLE
      - FAILED
      - ROLLED_BACK
      - SUPERSEDED
      - GONE
      - SCALED_TO_ZERO
      - DECOMMISSIONED
    PdDeploymentTarget:
      type: string
      enum:
      - ENVIRONMENT
      - PLATFORM
    PdEnvironmentDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        network:
          type: string
        platform:
          type: boolean
        createdAt:
          $ref: "#/components/schemas/Instant"
        applications:
          type: array
          items:
            $ref: "#/components/schemas/PdApplicationDto"
    PdLinkedServiceDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        target:
          $ref: "#/components/schemas/PdDeploymentTarget"
        availableOnEnv:
          type: boolean
        healthPath:
          type: string
    PdPinDto:
      type: object
      properties:
        applicationName:
          type: string
        shas:
          type: array
          items:
            type: string
    PdQualityGate:
      type: string
      enum:
      - UNMET
      - MET
    PdServiceDto:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        target:
          $ref: "#/components/schemas/PdDeploymentTarget"
        branch:
          type: string
        availableOnEnv:
          type: boolean
        healthPath:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        environmentIds:
          type: array
          items:
            type: string
    ScaleRequest:
      type: object
      properties:
        replicas:
          type: integer
          format: int32
    UpdateEnvironmentRequest:
      type: object
      properties:
        name:
          type: string
        platform:
          type: boolean
    UpsertServiceRequest:
      type: object
      properties:
        deploymentTarget:
          $ref: "#/components/schemas/PdDeploymentTarget"
        branch:
          type: string
        availableOnEnv:
          type: boolean
        healthPath:
          type: string
        environmentIds:
          type: array
          items:
            type: string
  securitySchemes:
    SecurityScheme:
      type: openIdConnect
      openIdConnectUrl: http://qits-platform-idp:8080/idp/.well-known/openid-configuration
      description: Authentication
paths:
  /platform-deployments/api/applications:
    get:
      summary: Every application deployed here — environment applications and platform
        services
      responses:
        "200":
          description: "The applications, one entry per tier"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListApplicationsResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Application Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/applications/{applicationId}/restart:
    post:
      summary: "Restart an application in place — its tasks are replaced, its deployment\
        \ is not"
      parameters:
      - name: applicationId
        in: path
        required: true
        schema:
          type: string
      responses:
        "202":
          description: Queued on the deploy worker
        "400":
          description: Not an application id
        "404":
          description: Nothing has ever been deployed for this application
        "409":
          description: Its newest deployment never reached the orchestrator
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Application Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/applications/{applicationId}/scale:
    post:
      summary: "Scale an application's workload — 0 stops it, 1 runs it"
      parameters:
      - name: applicationId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScaleRequest"
        required: true
      responses:
        "202":
          description: Queued on the deploy worker
        "400":
          description: "No replica count, a negative one, or one above the single\
            \ task every application here is deployed as"
        "404":
          description: Nothing has ever been deployed for this application
        "409":
          description: Its newest deployment never reached the orchestrator
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Application Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/deployment-requests:
    get:
      summary: "Deployment requests, newest-first — by environment, by project, or\
        \ by (repository, version)"
      parameters:
      - name: applicationName
        in: query
        schema:
          type: string
      - name: environmentId
        in: query
        schema:
          type: string
      - name: projectId
        in: query
        schema:
          type: string
      - name: repoId
        in: query
        schema:
          type: string
      - name: version
        in: query
        schema:
          type: string
      responses:
        "200":
          description: The requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeploymentRequestsResponse"
        "400":
          description: "No filter, or half of the repoId/version pair"
        "404":
          description: No such environment
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Deployment Request Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/deployment-requests/{id}:
    get:
      summary: "One deployment request, with the deployment it produced"
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentRequestResponse"
        "404":
          description: No such deployment request
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Deployment Request Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/deployments:
    get:
      summary: "One plane's recorded deployments, newest-first — an environment id,\
        \ or 'platform'"
      parameters:
      - name: environmentId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: The deployments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDeploymentsResponse"
        "400":
          description: environmentId was not given
        "404":
          description: No such environment
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Deployment Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/environments:
    get:
      summary: List environments
      responses:
        "200":
          description: The environments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListEnvironmentsResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
    post:
      summary: "Create an environment: a name and a bundle network"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEnvironmentRequest"
        required: true
      responses:
        "201":
          description: Created; a release may now be deployed into it
        "400":
          description: A name or network failed validation
        "409":
          description: An environment of that name already exists
        "401":
          description: Gate on and no machine token presented
        "403":
          description: Gate on and the token is for another service
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:system
  /platform-deployments/api/environments/{environmentId}:
    patch:
      summary: "Rename an environment, or designate it the platform environment"
      parameters:
      - name: environmentId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEnvironmentRequest"
        required: true
      responses:
        "200":
          description: The updated environment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentResponse"
        "400":
          description: A name failed validation
        "404":
          description: No such environment
        "409":
          description: "Another environment already has that name, or the platform\
            \ designation was cleared rather than moved"
        "401":
          description: Gate on and no machine token presented
        "403":
          description: Gate on and the token is for another service
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:system
    get:
      summary: One environment with the applications it tracks
      parameters:
      - name: environmentId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The environment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentResponse"
        "404":
          description: No such environment
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
    delete:
      summary: "Tear an environment down (rows, containers, networks)"
      parameters:
      - name: environmentId
        in: path
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Torn down
        "404":
          description: No such environment
        "409":
          description: This is the platform environment
        "401":
          description: Gate on and no machine token presented
        "403":
          description: Gate on and the token is for another service
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:system
  /platform-deployments/api/environments/{environmentId}/links:
    get:
      summary: "Every service present in this environment: its links, plus every platform\
        \ service"
      parameters:
      - name: environmentId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The services present in this environment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListLinksResponse"
        "404":
          description: No such environment
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Environment Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/pins:
    get:
      summary: "The image shas deployments pin: what serves, and what a rollback restores"
      responses:
        "200":
          description: One entry per application name serving somewhere; an application
            with no ACTIVE deployment pins nothing and is absent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPinsResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Pin Controller
      security:
      - SecurityScheme:
        - qits-platform:system
  /platform-deployments/api/services:
    get:
      summary: "Every service, with the environments each is linked into"
      responses:
        "200":
          description: The services
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListServicesResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pd Service Controller
      security:
      - SecurityScheme:
        - qits-platform:admin
  /platform-deployments/api/services/{name}:
    put:
      summary: "Register or update one service, replacing its environment links"
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertServiceRequest"
        required: true
      responses:
        "200":
          description: The updated service
          content:
            application/json:
              schema: {}
        "201":
          description: The newly registered service
        "400":
          description: "Validation failed, or a platform service was given environment\
            \ links"
        "404":
          description: An environmentId names no environment
        "409":
          description: A platform service cannot become an environment service
        "401":
          description: Gate on and no machine token presented
        "403":
          description: Gate on and the token is for another service
      tags:
      - Pd Service Controller
      security:
      - SecurityScheme:
        - qits-platform:system
    delete:
      summary: Remove a service and its links
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Removed
        "404":
          description: No such service
        "401":
          description: Gate on and no machine token presented
        "403":
          description: Gate on and the token is for another service
      tags:
      - Pd Service Controller
      security:
      - SecurityScheme:
        - qits-platform:system
info:
  title: qits-platform-deployments API
  version: 2026.904.183030
